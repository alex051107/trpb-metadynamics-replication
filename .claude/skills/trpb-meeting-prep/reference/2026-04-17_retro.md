# 2026-04-17 Meeting Prep Retro

## What worked

- 7 parallel agents (A/B/C/D/E/F/G) finished in ~5 min wall, none conflicted on files.
- Data-driven pptx generator (`generate_groupmeeting.js` + per-week data.json) cleanly swapped to black-white theme via 6-line COLOR constant change.
- Every external URL `curl -sI` verified before embedding → 5/5 live (PLUMED + GROMACS + DOI).
- sed-baked WALKERS_ID in multi_walker/plumed.dat + 3-stage sentinel verification = zero risk of per-walker identity mix-up.
- Script → scope → verification gates proved robust: each agent's report back was grep-able and independently checkable.

## What broke

1. **Codex rate-limited during dispatch** — Track E fell back to general-purpose. Mitigation: have both agent-types lined up; don't block on codex-rescue.
2. **TechDoc Ch8 named `select_snapshots.py` / `--array=0-9 submit.sh`** while Track E actually produced `setup_walkers.sh` + `submit_array.sh` (single multidir job). Discovered and fixed in Step 2 cross-coherence. **Lesson**: Step 2 is mandatory.
3. **User felt the 6-PDB Path CV projection sounded like repeat work** (he did plumed driver replay last week). Had to rewrite Chapter 5 intro + Slide 8 presenter script to contrast "last week = own trajectory self-consistency / this week = external PDB cross-species". **Lesson**: any "we did this again" framing must be explicit about what's new dimension.
4. **Meta**: `PIPELINE_STATE.json` had stale stage 4 blocking_issue still referencing Job 43813633 (already done) + `active_failure_patterns` missing FP-026/027. Fixed post-hoc. Track F should be MORE aggressive about updating PIPELINE_STATE next time.

## One-shot improvements for next week

- Add to agent prompts: "State explicitly for every analytic artifact you cite: is this NEW this week, or REPEAT of a prior check? If repeat, what's the new dimension (method / input / species / parameter regime)?"
- Pre-flight check before Step 1: diff `failure-patterns.md` against last week, feed diff into every agent prompt so they know which FPs are this-week's story.
- Pre-flight check before Step 1: confirm `PIPELINE_STATE.json` stages 3-4 match live Longleaf state (squeue output).

## Output quality bar

- TechDoc: `wc -l >= 800` and covers all 9 chapters
- PresenterScript: `wc -l >= 200` and has exactly one `## Slide N` per 10 slides
- pptx: `ls -la ... .pptx` size between 50 KB and 500 KB
- Every parameter in pptx has a matching row in `PARAMETER_PROVENANCE.md`
- Every external URL cited in pptx or TechDoc is `curl -sI` 2xx/3xx
