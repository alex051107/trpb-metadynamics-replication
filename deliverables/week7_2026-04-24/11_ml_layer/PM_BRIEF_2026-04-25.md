# PM Brief — ML Layer Design Round (2026-04-25)

> One page. The four artifacts below have already been written, audited, and red-teamed. This brief is **decision-only** — what PM has to sign off this week so the design unblocks.

```
┌──── ML LAYER DESIGN — 4 ARTIFACTS, 1 BRIEF ──────────────────────────┐
│ STATE (393 w)  ✅ digest of current ambition / decisions / blockers   │
│ INTERFACE v1.1 ✅ schemas + 14-col joint table + 3 fn sigs (Codex-fixed) │
│ PR_FAQ        ✅ Working-Backwards 1-pager: what V1 looks like @ 7-18 │
│ RED_TEAM      ✅ 8 attack vectors, 3 CRIT, 3 kill-switches, anti-overclaim │
│ Codex audit   ✅ 3 schema bugs caught + 5 secondary fixes inlined     │
└──────────────────────────────────────────────────────────────────────┘
```

---

## §0 TL;DR (read 30s, decide go/no-go)

V1 = **F0 (fast geometry) + PathGate (MetaD evidence) Evaluator**, ship target 2026-07-18 (~12 weeks).
- **What's ready now**: schemas, function signatures, demo command, anti-overclaim list — all four artifacts in `deliverables/week7_2026-04-24/11_ml_layer/`.
- **What's blocking**: PM hasn't picked the **activity proxy** (MMPBSA / k_cat / hand-bins). Without it, every L2/L3 supervised label is undefined.
- **What's at risk**: D-Trp mechanism transfer (silent killer #0). PathGate is calibrated on L-Trp; if generators target D-Trp, PathGate signal is uncorrelated with truth.
- **Recommendation**: ship V1 with the **3 kill-switches wired** (RED_TEAM §8) so any failure triggers degradation to F0-only research-mode rather than silent corruption.

---

## §1 What PM must decide this week (5 questions, ranked by leverage)

| # | Question | Default if no answer | Trigger |
|---|---|---|---|
| 1 | **Activity proxy**: (A) Yu's MMPBSA rank / (B) experimental k_cat where available / (C) hand-binned activity class? | block L2/L3 entirely — V1 ships descriptor-only | INTERFACE Q11 (added per Codex audit); STATE §3 #2 + #3 |
| 2 | **L2 scope**: (A) WT-Ain only / (B) WT × {Aex1, Q2} too / (C) ≥ 3 variants (GenSLM-230 + NdTrpB + WT)? | RED_TEAM §4 says (C) is ~600+ GPU-hr/variant + 3+ attempts/variant from FP history → unrealistic by week 10. Recommend **(A) commit week 1** | RED_TEAM §4 |
| 3 | **GenSLM populated or null in V1**? | If `genslm_embed = null` placeholder, V1 ships on time as F0+state-pseudo-labels; if must be populated, blocked on `d_model` + `pool` (BLOCKED #1 + #2 in INTERFACE §4) | INTERFACE Q5; PR_FAQ §4 fallback |
| 4 | **Reweighting estimator** for L2: (A) Tiwary-Parrinello c(t) / (B) WHAM / (C) MBAR? | (A) is canonical for our well-tempered + ADAPTIVE=DIFF setup per Codex audit; PM can default | INTERFACE Q7 |
| 5 | **Burn-in policy**: (A) flat 10 ns/walker / (B) adaptive s-histogram-plateau / (C) none? | (B) adaptive — flat default is bad for short pilots (Codex audit) | INTERFACE Q4 |

The other 6 INTERFACE Y/N questions can be defaulted (Polars, kcal/mol, 301×141 grid, etc.) per Codex priority annotation.

---

## §2 The 3 kill-switches (RED_TEAM §8) — PM signoff required

V1 ships in **degradation-ready state**: any of these trips → V1 auto-strips down to F0-only + PathGate research-mode. PM must approve the wiring NOW (cheap to add up-front, expensive to retrofit).

| Trip | Test | Action on trip |
|---|---|---|
| **D-Trp blindness** | Pick 2 D-divergent variants from Buller/Hilvert lit; if `\|Δs̄\|, \|Δz̄\|, \|ΔF_C\| < 1σ` on PathGate | Strip D-Trp claims; PathGate scope = L-Trp only |
| **MMPBSA-truth recursion** | n ≥ 12 paired (MMPBSA, k_cat); if Spearman \|ρ\| < 0.5 | Success metric pivots to bin-agreement; PathGate becomes descriptor-only |
| **PathGate–MMPBSA disjoint** | PathGate-top-3 ∩ MMPBSA-top-3 = ∅ on Yu's n=30 candidates AND #2 trips | V1 = F0-only; no joint reranker |

---

## §3 L0/L1/L2/L3 ladder — what unblocks today

| Tier | Today's status | Blocker |
|---|---|---|
| L0 (descriptors: s, z, time, walker) | ✅ usable from 19.8 ns pilot | none — INTERFACE §3.1 schema closed |
| L1 (state pseudo-labels: O / PC / C / off) | ✅ usable from same pilot | none — state_reason provenance added per Codex |
| L2 (FES values + state masks + convergence_grade) | ❌ blocked | needs 10-walker production (FP-031/032 history → 3+ attempts) + activity proxy + reweight ESS gate (RED_TEAM §6 — TP c(t) likely failing TODAY on the pilot) |
| L3 (generator-target scores) | ❌ blocked | needs L2 + ≥3 variant FES + activity proxy |

**V1 demo (PR_FAQ §1)**: `evaluator score-fasta GenSLM230.fasta --against WT_PfTrpB_FES_v1.npz` → CSV with `(sequence_id, F0_score, pathgate_score, combined_rank, confidence_band)`. PathGate column populated only if convergence_grade.status = PASS; otherwise EVAL_ONLY tag.

---

## §4 Anti-overclaim (PR_FAQ §5 + RED_TEAM §0) — must repeat to PI

Five things V1 **does NOT claim** (so PI doesn't read more than is there):
1. Not a converged WT FES — single-walker pilot is L0/L1 only.
2. Not D-Trp predictive — current path-CV is L-Trp closure geometry only (silent killer #0).
3. Not GenSLM-novelty validated — Lambert 2026 Fig 2A separates "natural vs generated", not "active vs inactive". Need ESM-2 / ProtBert-BFD ablation before claiming codon-level signal exists.
4. Not 3-variant — only WT pilot exists; Aex1 / Q2 / GenSLM-230 / NdTrpB FES are pending lab assets.
5. Not MMPBSA-validated — no published ρ(MMPBSA, TrpB k_cat); proxy itself is uncertain.

---

## §5 Where each artifact lives

```
deliverables/week7_2026-04-24/11_ml_layer/
├── STATE_OF_ML_THINKING_2026-04-25.md   (393 w  — START HERE)
├── INTERFACE_DESIGN_2026-04-25.md       (1500 w — schemas, sigs, 11 Q&A, errata)
├── PR_FAQ_2026-07-18_v1_ship.md         (697 w  — Working-Backwards memo)
├── RED_TEAM_ATTACK_2026-04-25.md        (1000 w — 8 vectors, kill-switches)
└── PM_BRIEF_2026-04-25.md               (this file — decision summary)
```

Read order for PM (~10 min total): this file → STATE digest → kill-switches in RED_TEAM §8 → 5 highest-leverage questions in §1 above. Schemas (INTERFACE) only when you delegate the build; PR_FAQ when you write the talk.

---

> Full audit chain: parallel sub-agent team (Architect + Blue-team) + Codex independent review (CCB `20260425-123133`). Three Codex-caught schema bugs already fixed in INTERFACE v1.1 errata. No schema is on the table that hasn't been independently challenged.
